import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// 렌더링 필요한 컴포넌트는 여기에 명시해야함 ㅇㅇ
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
